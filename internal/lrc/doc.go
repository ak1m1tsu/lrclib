// Package lrc implements parsing and generation of the LRC lyric file format.
// It handles both synced lyrics (timestamped lines) and plain lyrics, as well
// as the standard LRC metadata tags (ti, ar, al, length, by).
package lrc
